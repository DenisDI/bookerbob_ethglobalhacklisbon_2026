// Placeholder page. The race split-screen (SpentCounters, NarrationFeed,
// AddressBands, HotelFinaleCard, VerifyFlow) lands in later steps per
// specs/03-web-demo.md.
export function App() {
  return (
    <main
      style={{
        display: "grid",
        placeItems: "center",
        height: "100%",
        padding: "2rem",
      }}
    >
      <p
        style={{
          margin: 0,
          maxWidth: "24ch",
          fontSize: "clamp(1.5rem, 4vw, 2.75rem)",
          lineHeight: 1.2,
          letterSpacing: "-0.02em",
          textAlign: "center",
        }}
      >
        who is behind an agent changes the terms it gets
      </p>
    </main>
  );
}
