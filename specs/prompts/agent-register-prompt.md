Регистрируем кошелёк агента в World AgentBook. Сделай всю механику сам, я только
отсканирую QR в World App и апрувну. Канон: specs/01-gateway.md раздел AgentKit.

Контекст: у меня уже заведён World App (device-level, без орба), я апрувер.
Мой World-адрес: <вырезан, репозиторий публичный> (это ЧЕЛОВЕК, не агент).

Сделай по шагам, останавливаясь там, где нужно моё действие:

1. Сгенерь ОТДЕЛЬНЫЙ кошелёк агента (не мой World-адрес):
   `cast wallet new` (или через viem, если нет foundry).
   - Приватник запиши в .env как AGENT_PRIVATE_KEY (НЕ коммить, .env в gitignore).
   - Адрес агента 0x... покажи мне и запомни для следующего шага.

2. Запусти регистрацию:
   `npx @worldcoin/agentkit-cli register <адрес-агента>`
   - Команда попросит верификацию через World App (QR или deeplink).
   - ПОКАЖИ мне этот QR/ссылку крупно и ОСТАНОВИСЬ. Я сканирую своим World App
     (своим World-адресом) и апрувлю транзакцию на телефоне. Жди, не продолжай сам.

3. После моего апрува CLI отдаст tx hash (транзакция в World Chain):
   - Забери hash из вывода.
   - Впиши его в README.md вместо плейсхолдера [tx hash here].
   - Создай docs/AGENT-REGISTRATION.md: адрес агента, tx hash, ссылка на explorer
     World Chain, время (это доказательство, что ассет создан в окне хакатона).

4. Подключи РЕАЛЬНЫЙ верификатор вместо мока:
   - В src/world.ts замени mock AgentBookVerifier на createAgentBookVerifier из
     @worldcoin/agentkit-core (ончейн-lookup humanId по адресу агента).
   - Проверь e2e: запрос с валидным agentkit-заголовком от зареганного агента
     попадает в tier Human; запрос без заголовка остаётся Bot и идёт на x402.

5. Коммит: маленький, без длинного тире в месседже, обнови docs/AI-ATTRIBUTION.md
   в том же коммите, положи этот промпт в specs/prompts/. Перед коммитом git status,
   убедись что .env и приватник НЕ попали в индекс.

Если на шаге 2 CLI скажет, что нужен уровень верификации выше device-level,
остановись и скажи мне точно, что просит, я решу (NFC-паспорт на World-бусте,
без сканирования глаза).
