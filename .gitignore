# Pre-event research + local secrets — ignore everything listed below.
# Scratch app code will live outside these ignores when the build clock opens.

# --- archived pre-event research (not part of the submission specs) ---
# Supersedes the earlier per-file list: those files now live under specs/research/.
# The submission specs (00-final-plan, 01-03, CUT-ORDER, DEMO-IMPROVEMENTS, KEYS)
# stay tracked — 00-final-plan §F MUST 12 requires them in the repo.
specs/research/

# --- secrets & env ---
.env
.env.*
!.env.example
*.pem
*.key
secrets/
credentials/

# --- deps & build ---
node_modules/
dist/
build/
.turbo/
.next/
coverage/
*.log
.DS_Store

# --- local tooling ---
.vscode/
.idea/
*.swp
.cursor/
